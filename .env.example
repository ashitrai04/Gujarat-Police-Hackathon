# Mapbox GL access token (required) - https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=

# Backend REST + WebSocket base, e.g. http://localhost:8000
VITE_API_BASE_URL=

# "true" runs entirely on the bundled mock dataset - no backend needed.
VITE_USE_MOCKS=true

VITE_SENTINEL_HOST=https://live.corp8.cloud
VITE_ANPR_API_URL=
